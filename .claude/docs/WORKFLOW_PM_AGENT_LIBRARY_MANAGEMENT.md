# WORKFLOW: Управление библиотекой агентов для Project Manager

## 🎯 ЦЕЛЬ

PM должен уметь:
1. Проверять какие агенты уже созданы в библиотеке
2. Определять какой агент нужен для задачи
3. Создавать недостающего агента "в моменте"

---

## 📋 СПИСОК УНИВЕРСАЛЬНЫХ АГЕНТОВ

### Core Архитектурные агенты (Archon Team)
1. **Blueprint Architect** - архитектура и дизайн систем
2. **Analysis Lead** - анализ и декомпозиция требований
3. **Implementation Engineer** - базовая реализация кода
4. **Quality Guardian** - тестирование и контроль качества
5. **Deployment Engineer** - DevOps и развертывание

### Специализированные технические агенты
6. **Analytics Tracking Agent** - аналитика и трекинг
7. **API Development Agent** - разработка API (REST/GraphQL)
8. **Community Management Agent** - управление сообществом
9. **MCP Configuration Agent** - настройка MCP серверов
10. **Payment Integration Agent** - платежные интеграции (Stripe/PayPal)
11. **Performance Optimization Agent** - оптимизация производительности
12. **Prisma Database Agent** - работа с БД через Prisma ORM
13. **PWA Mobile Agent** - Progressive Web Apps и мобильная разработка
14. **Queue Worker Agent** - фоновые процессы и очереди
15. **RAG Agent** - поисковые системы с векторными БД
16. **Security Audit Agent** - аудит безопасности
17. **TypeScript Architecture Agent** - архитектура TypeScript проектов
18. **UIUX Enhancement Agent** - улучшение интерфейсов
19. **Pattern Orchestrator Agent** - координация pattern агентов для проекта PatternShift

---

## 🔍 WORKFLOW: Проверка и создание агента

### ШАГ 1: Получил задачу из Archon

```python
task = mcp__archon__find_tasks(task_id="...")
# Анализирую задачу
```

### ШАГ 2: Определить тип задачи и нужного агента

**Ключевые слова для определения:**

| Ключевые слова в задаче | Нужен агент |
|-------------------------|-------------|
| "архитектура", "дизайн системы", "blueprint" | Blueprint Architect |
| "анализ", "требования", "декомпозиция" | Analysis Lead |
| "код", "реализация", "программирование" | Implementation Engineer |
| "тест", "качество", "проверка" | Quality Guardian |
| "deployment", "ci/cd", "devops" | Deployment Engineer |
| "analytics", "tracking", "метрики" | Analytics Tracking Agent |
| "api", "endpoints", "rest", "graphql" | API Development Agent |
| "payment", "stripe", "paypal", "платеж" | Payment Integration Agent |
| "database", "prisma", "migration", "schema" | Prisma Database Agent |
| "performance", "оптимизация", "скорость" | Performance Optimization Agent |
| "security", "безопасность", "audit" | Security Audit Agent |
| "ui", "ux", "интерфейс", "дизайн" | UIUX Enhancement Agent |
| "typescript", "types", "архитектура TS" | TypeScript Architecture Agent |
| "pwa", "mobile", "offline" | PWA Mobile Agent |
| "queue", "worker", "background job" | Queue Worker Agent |
| "search", "rag", "vector", "embeddings" | RAG Agent |
| "mcp", "server", "конфигурация" | MCP Configuration Agent |
| "community", "модерация", "пользователи" | Community Management Agent |
| "pattern", "PatternShift", "психология", "трансформация" | Pattern Orchestrator Agent |

### ШАГ 3: Проверить наличие агента в библиотеке

```python
# Поиск агента по ключевому слову
result = Glob(
    pattern="**/[ключевое_слово]*agent*.md",
    path=".claude/agents/"
)

if result:
    # Агент найден
    agent_file = result[0]
    print(f"✅ Найден агент: {agent_file}")
else:
    # Агента НЕТ
    print(f"❌ Агент не найден в библиотеке")
```

### ШАГ 4а: ЕСЛИ АГЕНТ ЕСТЬ → Делегировать задачу

```python
mcp__archon__manage_task(
    "create",
    project_id=current_project_id,
    assignee="[Имя агента]",
    title="[Название задачи]",
    description="[Описание]",
    status="todo"
)
```

### ШАГ 4б: ЕСЛИ АГЕНТА НЕТ → Создать агента

```
🚨 СТОП - агента нет в библиотеке!

1. Переключиться в роль Blueprint Architect
2. Создать агента:
   - Найти промпт Blueprint Architect:
     Glob(pattern="**/blueprint-architect*.md")
   - Прочитать системный промпт
   - Объявить переключение пользователю
   - TodoWrite с микрозадачами создания агента

3. Blueprint Architect создает:
   - .claude/agents/[new-agent-name].md (~600 строк)
   - Системный промпт роли
   - Секция "ОБЯЗАТЕЛЬНО перед работой" (задача → Context7 → файлы)
   - Context7 query для примеров (НЕ сами примеры в промпте!)
   - "Когда использовать"
   - "Связанные агенты"

4. Вернуться в роль PM

5. Делегировать задачу новому агенту
```

---

## 📝 ПРИМЕР WORKFLOW В ДЕЙСТВИИ

### Сценарий 1: Агент существует

```
USER: "Нужно добавить Stripe payment в проект"

PM:
1. Анализирую: "payment", "stripe" → нужен Payment Integration Agent
2. Проверяю библиотеку:
   Glob(pattern="**/payment*agent*.md", path=".claude/agents/")
   → Найден: .claude/agents/payment-integration-agent.md
3. ✅ Агент существует!
4. Создаю задачу:
   mcp__archon__manage_task(
     "create",
     assignee="Payment Integration Agent",
     title="Интегрировать Stripe payment",
     status="todo"
   )
5. Уведомляю пользователя: "Задача делегирована Payment Integration Agent"
```

### Сценарий 2: Агента нет - создаем

```
USER: "Нужно настроить Kubernetes deployment"

PM:
1. Анализирую: "kubernetes", "deployment" → нужен Kubernetes Deployment Agent
2. Проверяю библиотеку:
   Glob(pattern="**/kubernetes*agent*.md", path=".claude/agents/")
   → НЕ найден
3. ❌ Агента нет в библиотеке!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 СТОП: Нужен новый агент
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4. Переключаюсь в роль Blueprint Architect

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎭 ПЕРЕКЛЮЧАЮСЬ В РОЛЬ BLUEPRINT ARCHITECT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 Моя экспертиза:
• Architecture design
• Technology stack selection
• System design patterns
• Agent creation

✅ Готов создать Kubernetes Deployment Agent

5. TodoWrite:
   - [1/5] Context7: "kubernetes deployment patterns"
   - [2/5] Создать .claude/agents/kubernetes-deployment-agent.md
   - [3/5] Написать системный промпт с экспертизой
   - [4/5] Добавить примеры K8s deployments
   - [5/5] Сохранить в библиотеку

6. Создаю файл kubernetes-deployment-agent.md...

7. Возвращаюсь в роль PM

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎭 ПЕРЕКЛЮЧАЮСЬ ОБРАТНО В РОЛЬ PROJECT MANAGER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

8. Делегирую задачу новому агенту:
   mcp__archon__manage_task(
     "create",
     assignee="Kubernetes Deployment Agent",
     title="Настроить K8s deployment для проекта"
   )
```

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА

1. **ВСЕГДА проверять библиотеку перед созданием**
   - Glob(pattern="**/*[keyword]*agent*.md", path=".claude/agents/")

2. **НЕ создавать дублирующих агентов**
   - Если есть похожий агент → использовать его
   - Если функционал близкий → расширить существующего

3. **Создание агента = переключение роли**
   - СТОП текущая работа
   - Переключиться в Blueprint Architect
   - Показать пользователю переключение
   - TodoWrite для создания
   - Вернуться в PM роль

4. **Список агентов НЕ ИСЧЕРПЫВАЮЩИЙ**
   - Если нужен новый тип специализации → создать
   - Библиотека растет органически

---

## 📊 КЛЮЧЕВЫЕ МЕТРИКИ

**Текущая библиотека:**
- Core агенты: 5
- Специализированные: 14
- **TOTAL: 19 универсальных агентов**

**Pattern агенты (управляются Pattern Orchestrator):**
- Pattern Orchestrator Agent - входит в универсальную библиотеку
- 17 pattern агентов для проекта PatternShift - управляются Orchestrator'ом
- PM делегирует задачи PatternShift → Pattern Orchestrator
- Pattern Orchestrator сам управляет своими 17 pattern агентами

---

## 🔄 ОБНОВЛЕНИЕ СПИСКА

Когда создан новый агент:
1. Агент автоматически появляется в `.claude/agents/`
2. PM находит его через Glob при следующей проверке
3. Список в этом файле - REFERENCE (не обязательный)
4. Источник истины - файловая система `.claude/agents/`

---

**Версия:** 1.0
**Дата создания:** 2025-11-06
**Для:** Project Manager (Archon Team)
