# Context7 Integration - Обязательное правило
---
## 🎯 КЛЮЧЕВОЕ ПРАВИЛО
**Context7 ВСЕГДА на шаге 2** - сразу после анализа задачи, ПЕРЕД написанием кода.
**НИКОГДА не писать код без Context7** - это ведет к устаревшим практикам и багам.
---
## 📋 Workflow поиска примеров
### 1. Определи технологию задачи
Примеры:
- "FastAPI authentication"
- "React hooks patterns"
- "Prisma migrations"
- "Stripe payment integration"
### 2. Найди library ID в Context7
```python
mcp__context7__resolve-library-id(
    libraryName="fastapi"  # или "react", "prisma", "stripe"
)
# Получишь: context7CompatibleLibraryID="/tiangolo/fastapi"
```
### 3. Получи документацию с примерами
```python
mcp__context7__get-library-docs(
    context7CompatibleLibraryID="/tiangolo/fastapi",
    topic="authentication jwt"  # конкретная тема
)
```
**Основной параметр:**
- `topic`: фокусировка на конкретную тему (например: "authentication jwt")
### 4. Используй примеры из документации
- ✅ Адаптируй под задачу
- ✅ Понимай паттерн и best practices
- ✅ Проверяй версию библиотеки
- ❌ НЕ копируй слепо
- ❌ НЕ игнорируй warning'и из документации
---
## ⚡ Когда использовать Context7
**ВСЕГДА ПЕРЕД:**
- Написанием нового кода
- Интеграцией библиотеки/API
- Решением проблемы/бага
- Выбором подхода реализации
- Рефакторингом существующего кода
- Добавлением новой зависимости
**ПРИМЕРЫ ЗАПРОСОВ:**
```python
# Backend development
"fastapi dependency injection examples"
"sqlalchemy async queries best practices"
"pydantic validation custom validators"
# Frontend development
"react hooks useEffect cleanup"
"next.js server components data fetching"
"typescript generics utility types"
# Database
"prisma relations many to many"
"postgresql indexing strategies"
"redis caching patterns"
# Testing
"pytest fixtures async"
"jest mock functions"
"playwright selectors best practices"
```
---
## 💡 Best Practices
### 1. Специфичные запросы
❌ Плохо: "react examples"
✅ Хорошо: "react hooks useState with typescript"
❌ Плохо: "fastapi docs"
✅ Хорошо: "fastapi background tasks celery integration"
### 2. Версионность
- Всегда проверяй версию библиотеки в проекте
- Если Context7 показывает новую версию - сверь с package.json/requirements.txt
- При несовпадении версий - ищи документацию конкретной версии
### 3. Комбинирование с existing code
```python
# WORKFLOW:
# 1. Context7 → примеры из документации
# 2. Grep/Read → существующие паттерны проекта
# 3. Комбинировать: библиотечные примеры + паттерны проекта
```
**Пример:**
```python
# Context7 показал: как работать с Stripe
# Grep показал: как в проекте обрабатываются платежи
# Результат: Stripe integration в стиле проекта
```
### 4. Валидация примеров
ВСЕГДА проверяй примеры из Context7:
- ✅ Актуальность (deprecated warnings?)
- ✅ Security (есть ли уязвимости?)
- ✅ Performance (оптимален ли подход?)
- ✅ Compatibility (подходит ли для проекта?)
---
## 🔄 Типичные сценарии использования
### Сценарий 1: Новая интеграция API
```python
# ЗАДАЧА: Интегрировать Stripe payment
# ШАГ 1: Context7
resolve-library-id("stripe")
get-library-docs("/stripe/stripe-python", topic="checkout session")
# ШАГ 2: Анализ примеров
# - Как создавать checkout session?
# - Как обрабатывать webhooks?
# - Как handle errors?
# ШАГ 3: Реализация
# Пишу код на основе примеров + паттернов проекта
```
### Сценарий 2: Решение бага
```python
# ПРОБЛЕМА: SQLAlchemy lazy loading N+1 problem
# ШАГ 1: Context7
get-library-docs("/sqlalchemy/sqlalchemy", topic="eager loading relationships")
# ШАГ 2: Найти решение
# Примеры: joinedload(), selectinload()
# ШАГ 3: Применить
# Использовать правильный eager loading
```
### Сценарий 3: Выбор подхода
```python
# ЗАДАЧА: Реализовать real-time notifications
# ШАГ 1: Context7 для сравнения подходов
get-library-docs("/fastapi/fastapi", topic="websockets")
get-library-docs("/fastapi/fastapi", topic="server sent events")
# ШАГ 2: Сравнить
# WebSockets: bi-directional, сложнее
# SSE: uni-directional, проще
# ШАГ 3: Выбрать под задачу
# Для notifications - SSE достаточно
```
---
## 🎓 Примеры Context7 queries для агентов
**В промптах агентов пишутся ТОЛЬКО query strings, НЕ код:**
```
"fastapi post endpoint validation"
"next.js server components examples"
"prisma transaction handling"
"react form validation patterns"
"stripe checkout session webhook"
"paypal api integration python"
"prisma schema relations"
"prisma migration rollback"
```
---
## ✅ Checklist перед написанием кода
```
□ Context7 запущен для нужной библиотеки?
□ Изучил примеры из документации?
□ Проверил версию библиотеки в проекте?
□ Сверил с существующими паттернами проекта?
□ Понял best practices и warnings?
□ Готов адаптировать примеры под задачу?
```
**Если хотя бы один пункт НЕ выполнен - НЕ начинай писать код!**
---
## 🚨 Частые ошибки
### Ошибка 1: Пропуск Context7
```python
# ❌ НЕПРАВИЛЬНО
"Задача понятна, начинаю писать код..."
# ✅ ПРАВИЛЬНО
"Задача понятна, СНАЧАЛА Context7 для актуальных примеров..."
```
### Ошибка 2: Слишком общий запрос
```python
# ❌ НЕПРАВИЛЬНО
resolve-library-id("python")
# ✅ ПРАВИЛЬНО
resolve-library-id("fastapi")
resolve-library-id("sqlalchemy")
```
### Ошибка 3: Игнорирование warnings
```python
# Context7 показал: "⚠️ This method is deprecated in v3.0"
# ❌ НЕПРАВИЛЬНО: использовать deprecated метод
# ✅ ПРАВИЛЬНО: найти актуальный аналог
```
### Ошибка 4: Слепое копирование
```python
# ❌ НЕПРАВИЛЬНО
"Скопировал пример из Context7 как есть"
# ✅ ПРАВИЛЬНО
"Адаптировал пример под архитектуру проекта и требования задачи"
```
